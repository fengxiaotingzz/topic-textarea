import React from "react";

import "./index.less";

export default function TopicList({ width = 0, height = 0 }) {
  return (
    <div style={{ left: width, top: height }} className="topic-list">
      topic
    </div>
  );
}
